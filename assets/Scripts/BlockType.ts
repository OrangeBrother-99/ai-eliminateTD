import { Color } from "cc";
// 在 Block.ts 中添加枚举

// 添加颜色常量配置
export const BlockColors = {
    COLORS: [
        Color.RED, // 红
        Color.GREEN, // 绿
        Color.BLUE, // 蓝
        Color.YELLOW, // 黄
        Color.WHITE  // 紫
    ]
};
